# SAM-425: Add AdminApp/utils/nta.py as the NTA client for cancellations

## TODO

### 1. `AdminApp/utils/env.py` → add `require_env()`: ✅
- **What:** Helper that raises if an env var is unset/empty.
- **Why:** `nta.py` needs it to load `NTA_BASE_URL`; doesn't exist in admin yet.
- **Who:** Us (admin backend).
- **When:** First — everything else depends on it.
- **Where:** `AdminApp/utils/env.py`
- **How:** Small function, mirror pattern used in customer backend's `require_base_url`.
- **How much:** Trivial, few lines.

```python

def require_env(key: str) -> str:
    """Return the env var's value, or raise RuntimeError if unset/empty."""
    value = os.getenv(key)
    if not value:
        raise RuntimeError(f"{key} is not set in environment variables")
    return value
```

### 2. `AdminApp/utils/nta.py` (new)
- **What:** `NTA_BASE_URL`, `NTA_PURCHASE_PATHS` (v2+v3), `NTA_HEADERS`, `send_to_nta()`.
- **Why:** Centralize NTA call so future v2/v3 branching has one place to plug into.
- **Who:** Us.
- **When:** After #1.
- **Where:** New file, `AdminApp/utils/`.
- **How:** Load `NTA_BASE_URL` via `require_env`, define both paths, build `send_to_nta(body, nta_version)`.
- **How much:** Small module.

### 3. Error handling in `send_to_nta()`
- **What:** Catch `RequestException` → 502. Log cert-expiry `ConnectionError` distinctly.
- **Why:** Today `send_post_request` lets raw exceptions through; cert expiry causes silent RST (別紙7-3), currently indistinguishable in logs.
- **Who:** Us.
- **When:** Same time as #2.
- **Where:** Inside `send_to_nta()`.
- **How:** try/except around the HTTP call.
- **How much:** Small, a few lines added to #2.

### 4. `routers/refund.py` → use `send_to_nta()`
- **What:** Replace inline URL-build/try-except in both cancel endpoints with a call to `send_to_nta()`.
- **Why:** Remove duplicated logic, existing v2 behavior must stay identical.
- **Who:** Us.
- **When:** After #2 and #3 exist.
- **Where:** `AdminApp/routers/refund.py`, both endpoints.
- **How:** Swap the inline block for one call.
- **How much:** Moderate — touches two endpoints, needs existing tests to still pass.

### 5. `conftest.py` → confirm `NTA_BASE_URL` setdefault
- **What:** Verify test env var still covers `nta.py`'s import-time read.
- **Why:** Import-time env reads break test collection if unset.
- **Who:** Us.
- **When:** Last, as a check after #1–#4.
- **Where:** `conftest.py` (already has the setdefault — just confirm it still applies).
- **How:** Run tests, no code change expected.
- **How much:** Trivial, verification only.
